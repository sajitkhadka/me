'use client';
import { Editor } from '@/editor';

const TemplateComponent = () => {
    return (
        <Editor
            onChange={() => { }}
        />
    );
};

export default TemplateComponent;
